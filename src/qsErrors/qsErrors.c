#include "qsErrors.h"

int qsErrors(int error, char* line, int len) {
    
    // Format error message based on error number
    switch(error) {
        case ARGS:
            break;
        case A_OUT:
            break;
        case B_OUT:
            break;
        case C_OUT:
            break;
        case FORMAT:
            break;
        case NULL_POINT:
            break;
        case PRODUCT:
            break;
    }
    
    return error;
}
